const castVote = (name, votes) => {

  if (name === 'Tim') {
    votes[0]++;
  return(votes);  
  } 
  else if (name === 'Sally') {
    votes[1]++; 
  return(votes);
  }
  else (name === 'Beth'); {
    votes[2]++;
  return(votes);
  }

};


